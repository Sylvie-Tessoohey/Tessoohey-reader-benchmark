[build-system]
requires = ["setuptools==80.9.0"]
build-backend = "setuptools.build_meta"

[project]
name = "tessoohey-reader-benchmark"
version = "0.1.0"
description = "Independent, evidence-aware evaluation of Module 1 documentary JSON"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
reader-benchmark = "reader_benchmark.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
